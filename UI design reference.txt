/* ================================
   TEXT SIZE + THINNER TYPOGRAPHY SYSTEM
   ================================
   Font: Poppins (300, 400, 500)
   Base: 16px
   ================================ */

/* 🔹 Page Header (Main Title)
   Example: "Employee Dashboard"
   Size: 24px (1.5rem)
   Weight: 500 (thinner)
*/
.page-header {
    font-size: 1.5rem;
    font-weight: 500;
}

/* 🔹 Section Title
   Example: Card titles, sections
   Size: 20px (1.25rem)
   Weight: 500
*/
.section-title {
    font-size: 1.25rem;
    font-weight: 500;
}

/* 🔹 Subsection Title
   Example: Smaller headings inside sections
   Size: 18px (1.125rem)
   Weight: 400
*/
.subsection-title {
    font-size: 1.125rem;
    font-weight: 400;
}

/* 🔹 Primary Text
   Example: Main content, paragraphs
   Size: 16px (1rem)
   Weight: 300 (thinner)
*/
.text-primary {
    font-size: 1rem;
    font-weight: 300;
}

/* 🔹 Secondary Text
   Example: Labels, descriptions
   Size: 14px (0.875rem)
   Weight: 300
   Color: softer
*/
.text-secondary {
    font-size: 0.875rem;
    font-weight: 300;
    color: rgba(255,255,255,0.75);
}

/* 🔹 Small / Meta Text
   Example: Timestamps, helper text
   Size: 12px (0.75rem)
   Weight: 300
*/
.text-meta {
    font-size: 0.75rem;
    font-weight: 300;
    color: rgba(255,255,255,0.6);
}

/* 🔹 Buttons */
button {
    font-size: 0.9rem;
    font-weight: 400;
}

/* 🔹 Input Fields */
input, select, textarea {
    font-size: 0.95rem;
    font-weight: 300;
}

/* 🔹 Topbar / Navbar Text */
.navbar-text {
    font-size: 0.95rem;
    font-weight: 400;
}

/* 🔹 Sidebar Menu Items */
.sidebar-item {
    font-size: 0.95rem;
    font-weight: 300;
}

/* 🔹 Big Numbers / Stats */
.stats-number {
    font-size: 1.75rem;
    font-weight: 500;
}

/* 🔹 Table Content */
table td {
    font-size: 0.875rem;
    font-weight: 300;
}

/* 🔹 Table Header */
table th {
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
}

/* ================================
   GLOBAL THINNER TEXT IMPROVEMENTS
   ================================ */

body {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    letter-spacing: 0.2px;
}

/* ================================
   UI COLOR SYSTEM (UNCHANGED)
   ================================ */

/* PRIMARY BRAND COLOR */
--primary-color: #97be41;

/* TEXT COLORS */
--text-lightest: #ffffff;
--text-light: rgba(255, 255, 255, 0.85);
--text-muted: rgba(255, 255, 255, 0.55);
--text-dark: #333333;
--text-dark-muted: #666666;

/* BACKGROUNDS */
--bg-light: #ffffff;
--bg-soft: #f5f5f5;
--bg-dark: #1e1e1e;

/* GLASS / SIDEBAR */
--glass-bg: rgba(0, 0, 0, 0.85);
--glass-border: rgba(255, 255, 255, 0.085);
--glass-top: rgba(255, 255, 255, 0.16);
--glass-hover: rgba(255, 255, 255, 0.08);
--sidebar-glow: 0 0 0 1px rgba(255,255,255,0.055),
                6px 0 48px rgba(0,0,0,0.55),
                inset -1px 0 0 rgba(255,255,255,0.04);

/* STATE COLORS */
--success: #97be41;
--danger: #dc3545;

/* BORDERS */
--border: rgba(0, 0, 0, 0.1);

/* SHADOWS */
--shadow: rgba(0, 0, 0, 0.15);

/* SELECTION */
::selection {
    background: #97be41;
    color: #ffffff;
}